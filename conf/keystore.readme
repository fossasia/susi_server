the keystore was generated with
keytool -genkey -alias sitename -keyalg RSA -keystore keystore.jks -keysize 2048
the password is 123456

you can customize/replace the keystore. Just re-run the keytool, use a different
password and then set the password in the properties file.